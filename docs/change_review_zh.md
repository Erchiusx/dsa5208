# 给队友的更新：最终采用 316 组

可以直接转述：

> 最后保留 316 组完整实验，覆盖 4 种性质 × 3 个一致性级别 × 3 种场景，每个组合 8–9 次。原先计划 1,080 组，两次在正式实验前的准备阶段中断，因此没有把未执行的组算进去。报告采用实际的 172 PASS、38 VIOLATION、106 INCONCLUSIVE，也保留三组与预测不一致的结果。每组都有操作、故障和恢复记录。

## 相对 Erchius 原代码，之前的修正

原项目结构和轨迹执行/checker 架构保留。此前修正集中于 MW/WFR 依赖证据、WFR 写入者必须实际读过前驱、去除 seq 排序代表内部执行顺序的误判、分离业务写和审计写结果、MR 先读到新值再检查回退，以及验证节点间分区和客户端连通性。MW/WFR 使用局部前驱可见性检查，报告明确了它的局限。

## 本次整理的范围

- 没有再次修改 src/ 核心代码或原有工作负载/控制辅助实现；它们与两阶段实际执行的快照一致。
- 归档 results/final-316-20260924/，原样保留 316 组、来源哈希、执行过的两个源代码快照和被排除的准备失败记录。
- 新增 summarize_completed.py，离线核验已有数据并生成汇总。
- 将执行过的独立流程包装脚本整理为 reproduce_independent.py，复用现有实验逻辑，可选择 36/316 组；统一使用第二阶段准备策略。此次没有新跑数据库实验。
- PDF、README 和中文说明统一为 316 组；保留 Jia Siqi、Jia Zeyue 和 ElegantPaper 模板。

没有把未执行的额外健康探针、自动集群重启方案加入提交。历史计划中的 1,080 只是来源记录，不是完成数量。316 组核验和原有 28 个单元测试通过；新入口的参数及顺序已验证，但没有实际重跑一轮。

两阶段准备策略不同、截止点事后决定；局部 ONE 探针证据与 QUORUM 客户端读取不是同一回事。这些局限都写进报告，不能将结果解读成普遍保证。
